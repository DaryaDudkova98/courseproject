# yaml-language-server: $schema=../vendor/symfony/routing/Loader/schema/routing.schema.json

# This file is the entry point to configure the routes of your app.
# Methods with the #[Route] attribute are automatically imported.
# See also https://symfony.com/doc/current/routing.html

# To list all registered routes, run the following command:
#   bin/console debug:router

controllers:
    resource: ../src/Controller/
    type: attribute

salesforce:
    resource: '../src/Controller/SalesforceController.php'
    type: attribute
    prefix: /
    
#connect_google:
    #path: /connect/google
    #controller: App\Controller\SecurityController::connectGoogle

#connect_google_check:
    #path: /connect/google/check

#connect_facebook:
    #path: /connect/facebook
    #controller: App\Controller\SecurityController::connectFacebook

#connect_facebook_check:
    #path: /connect/facebook/check

