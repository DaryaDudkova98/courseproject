{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content %}
    <h2>Search results: "{{ query }}"</h2>
    <ul>
        {% for user in results %}
            <li>{{ user.username }} — {{ user.email }}</li>
        {% else %}
            <li>Nothing found</li>
        {% endfor %}
    </ul>
{% endblock %}
